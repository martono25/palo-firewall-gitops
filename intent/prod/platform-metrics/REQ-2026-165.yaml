# GENERATED by `fwgitops from-issue` from https://github.com/martono25/palo-firewall-gitops/issues/165
#
# Edit it like any other intent — it is a normal request file from here on.
# Changing `spec` needs a NEW `metadata.ticket`; the pipeline rejects a
# modified rule that still carries the ticket which authorised the old one.
apiVersion: fw-intent/v1
kind: AccessRequest
metadata:
  id: REQ-2026-165
  requester: martono25
  ticket: JIRA-9301
  justification: Retest of the day-2 path on the rebuilt firewall 007955000902404.
    The metrics collector on the local segment needs to reach the syslog receiver
    in the DMZ.
  requested: '2026-08-12'
spec:
  environment: prod
  action: allow
  source:
  - cidr: 10.100.3.40/32
  destination:
  - cidr: 10.100.1.60/32
  service:
  - protocol: tcp
    port: '6514'
  log: true
