# GENERATED by `fwgitops from-issue` from https://github.com/martono25/palo-firewall-gitops/issues/152
#
# Edit it like any other intent — it is a normal request file from here on.
# Changing `spec` needs a NEW `metadata.ticket`; the pipeline rejects a
# modified rule that still carries the ticket which authorised the old one.
apiVersion: fw-intent/v1
kind: AccessRequest
metadata:
  id: REQ-2026-152
  requester: martono25
  ticket: JIRA-9100
  justification: 'Day-2 verification on the rebuilt pilot: prove the requester path
    reaches firewall 007955000901881 end to end.'
  requested: '2026-08-11'
spec:
  environment: prod
  action: deny
  source:
  - cidr: 192.0.2.0/24
  destination:
  - cidr: 198.51.100.30/32
  service:
  - protocol: tcp
    port: '9500'
  log: true
