# GENERATED by `fwgitops from-issue` from https://github.com/martono25/palo-firewall-gitops/issues/121
#
# Edit it like any other intent — it is a normal request file from here on.
# Changing `spec` needs a NEW `metadata.ticket`; the pipeline rejects a
# modified rule that still carries the ticket which authorised the old one.
apiVersion: fw-intent/v1
kind: AccessRequest
metadata:
  id: REQ-2026-121
  requester: martono25
  ticket: JIRA-9001
  justification: 'Intake end-to-end verification: deny egress from the TEST-NET-1
    range to a documentation address.'
  requested: '2026-08-10'
spec:
  environment: prod
  action: deny
  source:
  - cidr: 192.0.2.0/24
  destination:
  - cidr: 198.51.100.7/32
  service:
  - protocol: tcp
    port: '8443'
  log: true
